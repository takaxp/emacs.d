This git repository stores files of configurations for Emacs 23
The author is Takaaki ISHIKAWA <takaxp@ieee.org>

Call the emacs lisp files in your .emacs file as follows.
------------------------
(require 'takaxp.el)
(require 'takaxp-utility)
(require 'takaxp-org-mode)
(when (eq window-system 'ns)
  (require 'takaxp-mac))
(require 'takaxp-keybinding)
------------------------

  - STRUCTURE
	./takaxp.el             Main file and set core configurations to use Emacs
	./takaxp-utility.el     Defines functions to use Emacs handy
	./takaxp-org-mode.el    Configurations for Org-mode
	./takaxp-mac.el         Configurations to use Emacs on Macintosh
	./takaxp-keybinding.el  Global settings of keybindings
	
